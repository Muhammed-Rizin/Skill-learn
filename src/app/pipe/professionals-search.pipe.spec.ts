import { ProfessionalsSearchPipe } from './professionals-search.pipe';

describe('ProfessionalsSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfessionalsSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
